{
  "name": "netlify-nest",
  "version": "1.0.0",
  "description": "Netlify monorepo nest example",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:api": "yarn workspace @netlify-nest/rest build",
    "start:api": "yarn workspace @netlify-nest/rest start",
    "build:shop": "yarn workspace @netlify-nest/shop build",
    "start:shop": "yarn workspace @netlify-nest/shop start"
  },
  "workspaces": [
    "api/*",
    "shop"
  ],
  "author": "",
  "license": "ISC"
}
